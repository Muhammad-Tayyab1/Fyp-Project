{
  "name": "Fyp-Project",
  "version": "1.0.0",

  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch client --exec babel-node Server/server.js",
    "build": "rm -rf dist && babel Server -d dist  && cd client && npm install && npm run build",
    "heroku-postbuild-x": "npm run build && cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.794.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.2.0",
    "env": "0.0.2",
    "express": "^4.17.1",
    "express-async-handler": "^1.1.4",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.6.3",
    "mongoose": "^5.10.15",
    "multer": "^1.4.2",
    "multer-s3": "^2.9.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "@babel/node": "^7.12.6",
    "@babel/preset-env": "^7.12.1",
    "nodemon": "^2.0.6"
  }
}
